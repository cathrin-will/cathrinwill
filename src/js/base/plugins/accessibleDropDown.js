/*------------------------------------*\
accessible dropdown
\*------------------------------------*/
function accessibleDropDown() {
	// if (Modernizr.mq('only screen and (min-width: 1024px)')) { // only bigger than ipad
	// 	console.log(0)
	// 	$('.nav--site').on('focus', 'li:not(:last-child)', function() {
	// 		$(this).addClass('nav__active');
	// 	}).on('blur', 'li:last-child', function() {
	// 		$(this).removeClass('nav__active');
	// 		$(this).parent().parent().removeClass('nav__active');
	// 	});
	// }
}
